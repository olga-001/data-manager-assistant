### 🧭 Как читать эту diagram

Диаграмма отражает **реальный production-workflow**, реализованный в n8n, и читается **слева направо**.

**Основные принципы чтения:**

- **Каждый блок** соответствует логическому этапу обработки данных или группе нод в workflow.
- **Стрелки** показывают направление потока данных и управления.
- Диаграмма построена по принципу **одного объекта = один проход по цепочке**.

**Ключевые этапы:**

1. **Notion Database → Workflow Trigger**  
   Workflow стартует по карточкам со статусом `Ready to process`.

2. **Loop / Condition Check**  
   Каждая карточка обрабатывается отдельно в цикле.

3. **Unified Input Builder**  
   Все входные данные (текст, скриншоты, фотографии) объединяются и нормализуются в единый объект.

4. **Entry Assistant (AI Agent #1)**  
   Агент извлекает и структурирует фактические данные в строгий JSON.

5. **Location Assistant (AI Agent #2)**  
   Второй агент формирует нейтральное описание, используя результаты первого агента и исходный контекст.

6. **Merge & Validation Layer**  
   Данные проверяются и отбираются по правилу:  
   *обновлять только пустые и разрешённые поля*.

7. **Notion Update Payload**  
   Формируется безопасный payload без риска перезаписи существующих данных.

8. **Update & Status Management**  
   - при успехе: статус → `Ready to check`  
   - при ошибке: статус → `Error` + лог ошибки

9. **Loop Back**  
   После обработки карточки workflow возвращается к началу для следующего объекта.

Диаграмма демонстрирует **контролируемое использование LLM**, где модель является частью бизнес-процесса, а не источником неконтролируемых изменений.
